# Rules

案件で**継続的に守る制約・規約**を構造化して管理するディレクトリ。**1ルール1ファイル**で管理する。

「本番リリースは金曜禁止」「決済処理は必ず二重確認を通す」のような、**その後もずっと効き続ける約束事**を Gold 層のエンティティにする。AI エージェントが作業するとき、この制約を知らずに動くと事故になる（禁止された曜日にデプロイ手順を案内する等）。ルールを機械可読なレコードにしておくことで、巡回エージェント・AIS が案件の「守るべき線」を frontmatter から把握できる。

## Decision との違い

Decision（`Cortex/Decisions/`）と Rule は似ているが役割が違う。

| | Decision | Rule |
| --- | --- | --- |
| 何を記録するか | **経緯のある一回の決定**（いつ・誰が・なぜ決めたか） | **継続的に守る制約**（今も効いている約束事） |
| 時制 | 過去の出来事（凍結・追記型） | 現在も有効な規範（無効化されるまで生きる） |
| 何についてか | **どう作るか・どう構成するか** | **作業するときに守る運用上の制限** |
| 破るとどうなるか | 方針とズレる・作りが揃わない | **障害・情報漏洩・顧客との約束違反** |
| 典型 | 「Next.js を採用した」 | 「本番リリースは金曜禁止」 |

**「継続的に適用されるか」だけで判定しない。** 体制・構成・作り方の取り決めは、「必ず」「〜しない」と書かれ、ずっと適用されるものであっても **Decision** の領分。

- ✗ Rule にしない … 「各ハーネスにプレイブック層を必ず設ける」「新規案件にPPOを必ず配置する」「顧客共有Driveは案件ごとに1つ作る」「テンプレートは○○ディレクトリに配置する」
- ⭕️ Rule にする … 「本番リリースは金曜・祝前日に行わない」「顧客環境へのアクセスはVPN経由のみ」「個人情報を含むデータをローカルに保存しない」「決済処理は必ず二重確認を通す」

判定は **「破ったときに何が起きるか」** で行う。実害（障害・漏洩・約束違反）が説明できるものだけが Rule。

**0件が正常な案件もある。** 作るものが決まっていて運用上の制限が少ない案件では該当が無い。数を揃えるために Decision を言い換えて並べると、本当に守るべき線が埋もれる。

Decision から蒸留された Rule（「金曜リリース禁止を決定した」という Decision があり、そこから「金曜リリース禁止」という Rule を立てる）は、`relations` の `derived_from` で元 Decision を指す。

## 命名規則

実データは `records/` 配下に置き、ファイル名は `records/{slug}.md`。ID は `rule:{slug}` となる（例: `rule:本番リリース-金曜禁止`）。

## スキーマ

```yaml
---
type: rule
id: "rule:本番リリース-金曜禁止"
title: "本番リリースは金曜・祝前日に行わない"
description: "障害対応の人員が手薄になる週末直前の本番デプロイを禁止する運用ルール"
status: active          # draft（AI生成・未確認） | active（確認済み）
relations:              # 任意。元になった Decision 等
  - rel: derived_from
    target: "20260610-001"
---

（制約の本文。下の template.md の構成に従う）
```

| フィールド | 必須 | 説明 |
|-----------|------|------|
| type | ✅ | 常に `rule` |
| id | ✅ | `rule:{slug}` |
| title | ✅ | 制約の1行表現 |
| description | ✅ | 制約の1文要約（一覧・検索・外部消費で使う） |
| status | ✅ | `draft`（AI生成・人間未確認）/ `active`（確認済み） |
| relations | - | 元 Decision を `derived_from` で、関連ルールを `relates_to` で指す（任意）。`target` には**安定ID**（決定ID `YYYYMMDD-NNN`・`rule:{slug}` 等）を書き、**ファイルパスは使わない** |

## 内部情報は書かない（顧客開放の境界）

本リポジトリは顧客が直接アクセスする前提で運用する。制約の本文に**売上・原価・見積・単価・アサイン工数・人事評価・顧客/ベンダーへの率直な評価といった社内限定の情報を混ぜない**。判断に迷う情報はそもそも置かないこと。

## 運用（事後レビュー方式）

用語集（term）と同じ**事後レビュー方式**で運用する。

- **AI が起票したルールは必ず `status: draft`**（人間未確認の印）。レビューで内容を確認し `active` に変える
- **`status` は確認状態の印**。人が内容を伝えて起票したもの・人がレビューしたものは `active` でよい。`draft` は**AIが自動抽出して人がまだ見ていない**レコードだけに付ける
- 自動化の範囲は「新規追加」のみ。**既存レコードの自動書き換えは行わない**
- 効かなくなったルールは削除せず、置き換える Decision から履歴をたどれるようにする（本文に失効を追記するか、レビューで扱いを決める）
- push 前にスキーマ検証（エンジンの `validate-cortex`）が走るため、規約違反のレコードが main に入ることはない

> 夜間の `update-gold`（Phase D）が、その日の差分ソースから継続的な制約だけを**保守的に**抽出し `status: draft` で起票する（用語より一段厳しい基準。一回の決定・単発TODO・推測は起票しない）。人間がレビューして `active` に変える。人間・スキルからの手動起票も併用できる。

## 除外ルール

自動抽出（`update-gold` の Phase D）が**起票してはいけない制約**のリスト。レビューで却下した制約をここに追記すると、以後再追加されなくなる（編集は人間のみ。自動抽出はこのリストを読むだけで編集しない）。用語集の「除外用語」と同じ仕組み。

<!-- 1行1件で列挙する。例:
- 本番リリースは金曜禁止
- 全PRに2名レビュー必須
-->
