---
name: sync-materials
description: >-
  共有資料/配下のローカルファイル（Excel、PDF、PPTX、DOCX等）をMarkdownに変換して同期する。「資料を変換して」「資料を同期して」「ファイルを保存して」「Materialsに保存して」「資料を読み込めるようにして」「資料を更新して」などの依頼時に使用する。外部サービス（Google
  Drive等）からの取得は行わない。資料は手元にダウンロードするか 共有資料/ に置き、そのパスを渡して変換する。
allowed-tools:
  - Bash(uv run *)
  - Bash(mkdir -p *)
  - Bash(mv *)
  - Bash(cp *)
  - Bash(ls *)
  - Bash(rm *)
  - Bash(git stash *)
  - Bash(git pull *)
  - Bash(git push *)
  - Bash(git add *)
  - Bash(git commit *)
  - Bash(git status *)
---
# Sync Materials

共有資料/ 配下の資料を Markdown に変換し、AI が読みやすい形で同期する。

**外部サービス（Google Drive 等）からの取得は行わない。** 資料は手元にダウンロードするか `共有資料/` に置いたうえで、**そのパスを渡して**変換する。

## 2 つの使い方

ユーザーの入力から、以下の 2 パターンを判別して処理する。

| #   | パターン       | 入力                                          | 用途                                             |
| --- | -------------- | --------------------------------------------- | ------------------------------------------------ |
| 1   | **取り込み**   | 手元/外部のファイルパス（例: `~/Downloads/会社紹介.pdf`） | `共有資料/` 配下にコピーして変換（元ファイルは残す） |
| 2   | **整理・変換** | `共有資料/` に置いたファイル or ディレクトリ  | その場で整理して変換（ディレクトリ指定で再帰一括） |

---

## スクリプトの場所と実行方法

変換スクリプトは**この Skill に同梱**されている（`scripts/convert.py`）。依存（markitdown 等）はスクリプト先頭の PEP 723 インラインメタデータで自己完結しており、`uv run --no-project` で隔離環境に自動解決される。**リポジトリの `pyproject.toml` には依存しない**ため、Skill ごと他リポジトリへコピーしても動く。

実行時は、Skill 起動時に提示される **「Base directory for this skill」** の絶対パスを `<SKILL_DIR>` として使う。`共有資料/...` のパスはリポジトリルート（カレントディレクトリ）基準。

```bash
uv run --no-project "<SKILL_DIR>/scripts/convert.py" <引数...>
```

---

### パターン 1：取り込み（手元のファイル → 共有資料/）

手元（ダウンロードフォルダ等）にあるファイルを `共有資料/` 配下に取り込みつつ変換する。`--dest` に配置先の親ディレクトリ、`--name` に簡潔な名前を渡す。

```bash
uv run --no-project "<SKILL_DIR>/scripts/convert.py" "~/Downloads/会社紹介資料.pdf" --dest "共有資料/" --name "クラスメソッド会社紹介資料"
```

これで `共有資料/クラスメソッド会社紹介資料/{クラスメソッド会社紹介資料.pdf, .md}` が作られる（元ファイルはコピーなので手元にも残る）。`--name` を省略すると元ファイル名がそのまま使われる。

**命名ガイド（`--name` に渡す名前）**

- 資料の中身を端的に表す名前にする（フォルダ名＝ファイル名＝参照ID `material:{slug}` の元になる）
- 次のようなノイズは落とす: 用途・配布先の接頭辞（`〇〇用_`）、日付・版番号（`_v2`・`20260617`）、`コピー`・`最新版`・`(1)` 等
- 拡張子は付けない（スクリプトが補う）。スペースより中黒・アンダースコアを使い、パスとして扱いやすくする

### パターン 2：整理・変換（共有資料/ に置いたものを変換）

`共有資料/` に直接置いたファイルを、その場で整理（同名ディレクトリ作成＋移動）して変換する。

```bash
uv run --no-project "<SKILL_DIR>/scripts/convert.py" "共有資料/会社紹介資料.pdf" --organize
```

親ディレクトリを指定すると、配下を**再帰的に**走査し、未整理の対応ファイルをまとめて変換する。

```bash
uv run --no-project "<SKILL_DIR>/scripts/convert.py" "共有資料/" --organize
```

---

### ステップ：PDF・PPTX は Claude が変換（重要）

`.xlsx` / `.docx` はスクリプトが markitdown で自動変換するが、**PDF と PPTX は markitdown を使わない**。これらは図・スライドのレイアウトが崩れるため、**Claude 自身が読み込んで Markdown 化する**。

スクリプト実行後、出力に次の行があれば対応する：

```
[CLAUDE_TODO] .pptx はClaudeが読み込んでMarkdown化します。
  入力ファイル: <...>.pptx
  出力MD : <...>.md
  読み込み方法: pptx ドキュメントスキル（document-skills:pptx）で読み込む
```

このとき、`読み込み方法` に従って入力ファイルを読み込む：

- **PDF**: `Read` ツールで「入力ファイル」を読み込む（ページ数が多い場合は分割して読む）
- **PPTX**: `document-skills:pptx` スキルを使って「入力ファイル」のテキスト・スピーカーノート・表を抽出する

読み込んだら、共通で以下を行う：

1. 図・グラフは**何を表しているかを文章で説明**して Markdown 化する（「イメージ」等のプレースホルダにしない）
2. 見出し階層・表・強調を適切に再構成し、ページ番号などのノイズは除く
3. PPTX のスライドは見出しで区切り、スピーカーノートがあれば併記する
4. frontmatter は**付けない**（frontmatterを持つのはGold層のみ＝オントロジー規約）。この資料への参照ID `material:{出力mdファイル名（拡張子なし）}` はファイル名から機械的に導出されるため、メタデータの付与は不要
5. 「出力 MD」のパスに `Write` で保存する

> スクリプトはディレクトリ・実ファイルまで用意済み。Claude は `.md` の生成だけを担当する。

## 補足：スクリプトの自動処理

- **取り込み（--dest）**: 手元のファイルを `<親dir>/<--name または元ファイル名のstem>/` にコピー → 変換（PDF・PPTX は `[CLAUDE_TODO]` を出力して委譲）。元ファイルは手元にも残る。
- **整理・変換（--organize）**: 拡張子を除いた名前のディレクトリを作成 → 元ファイルを移動 → 変換 → `.md` を保存。ディレクトリ指定時は配下の未整理ファイルを再帰的に処理。
- **単発変換（-o / 引数のみ）**: `.xlsx` / `.docx` を md にして指定パスへ保存、または標準出力。
- **PDF・PPTX の変換**: markitdown は使わず、Claude が読み込んで `.md` を生成する（PDF は `Read` ツール、PPTX は `document-skills:pptx` スキル）。

```
共有資料/
  提案書/                  # 配置先フォルダ（直下にファイルを置く）
    ○○提案書.md           # 変換後Markdown
    ○○提案書.pptx         # 実データ（残す）
  外部API/
    △△API仕様.md
    △△API仕様.pdf
```

## 対応形式

- `markitdown`（Microsoft 製）で自動変換: `.xlsx`, `.docx`
- **Claude が読み込んで変換**: `.pdf`（`Read` ツール）, `.pptx`（`document-skills:pptx` スキル）
